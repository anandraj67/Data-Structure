package thread.questions.file_blocks;

public class BlockBytes {
    Block block;
    byte [] bytes;

    public BlockBytes(Block block, byte[] bytes) {
        this.block = block;
        this.bytes = bytes;
    }
}
